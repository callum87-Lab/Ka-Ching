services:
  kaching:
    build: .
    container_name: kaching
    restart: unless-stopped
    ports:
      - "8091:8000"
    volumes:
      - ./data:/data
    environment:
      # Flat estimate added per distinct release date (i.e. per likely shipment)
      - SHIPPING_ESTIMATE=4.00
